# Contributing

Use a feature branch, keep backend contracts backward compatible, and add focused tests for every change. Do not commit virtual environments, caches, secrets, generated builds, or machine-specific configuration. Run the backend test suite before opening a pull request.
