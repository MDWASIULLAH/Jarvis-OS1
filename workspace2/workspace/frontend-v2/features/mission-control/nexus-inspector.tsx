"use client";
import type { NexusNode } from "./types";
export function NexusInspector({ node, onClose }: { node?: NexusNode; onClose: () => void }) { return <aside className="nexus-inspector">{node ? <><header><div><span>{node.kind}</span><h3>{node.label}</h3></div><button onClick={onClose} aria-label="Close node inspector">×</button></header><dl><div><dt>Node ID</dt><dd>{node.node_id}</dd></div><div><dt>Status</dt><dd>{node.status ?? "Unavailable"}</dd></div><div><dt>Health</dt><dd>{node.health ?? "Unavailable"}</dd></div><div><dt>Created</dt><dd>{node.created_at ? new Date(node.created_at).toLocaleString() : "Unavailable"}</dd></div><div><dt>Last update</dt><dd>{node.updated_at ? new Date(node.updated_at).toLocaleString() : "Unavailable"}</dd></div></dl><section><h4>Metadata</h4>{node.metadata?.length ? node.metadata.map(item => <p key={item.key}><strong>{item.key}</strong> {item.value}</p>) : <p>Dependencies, activity, logs, metrics, links, and execution history are unavailable for this node.</p>}</section></> : <p className="mission-unavailable">Select a live Nexus node to inspect its operational context.</p>}</aside>; }
