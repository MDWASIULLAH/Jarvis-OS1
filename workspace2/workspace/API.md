# API

The current chat contract is `POST /v1/chat` with `text`, optional provider, and attachments encoded as `{ name, media_type, base64 }`. Health is available at `GET /health`. Preserve existing API routes and consult backend route definitions for the complete live contract.
